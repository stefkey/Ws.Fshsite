##
# One-Level-Menu
#
prototype(Site:OneLevelMenu) < prototype(Neos.NodeTypes:Menu) {
	attributes.class = 'container sj-small-container'
    maximumLevels = 1
    startingPoint = ${q(site).children('gruppen').get(0)}
}			